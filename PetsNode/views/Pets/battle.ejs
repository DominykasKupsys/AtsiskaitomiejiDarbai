<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BATTLE</title>
    <base href="http://localhost:3000/">
  </head>
  <body>
    <table>
      <thead>
        <th>Name</th>
        <th>Foto</th>
      </thead>
      <tbody>
        <tr>
          <td><%= pet1.name %></td>
          <td><a href="pets/result/<%= pet1.ID %>"><img id="<%= pet1.ID %>" src="images/<%= pet1.foto ?? 'nofoto.png' %>" style="max-height: 40px;"></a></td>
        </tr>
        <tr>
            <td><%= pet2.name %></td>
            <td><a href="pets/result/<%= pet2.ID %>"><img id="<%= pet2.ID %>" src="images/<%= pet2.foto ?? 'nofoto.png' %>" style="max-height: 40px;"></a></td>
          </tr>
      </tbody>
    </table>
  </body>
</html>